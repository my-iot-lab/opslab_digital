-- 后端
后端基于 

-- 前端
前端基于 vue-next-admin 框架开发，地址：https://lyt-top.gitee.io/vue-next-admin-doc-preview

# 安装 cnpm
npm install -g cnpm --registry=https://registry.npm.taobao.org，以后安装直接采用cnpm替代npm

# 安装依赖
cnpm install

# 运行项目
cnpm run dev

# 打包发布
cnpm run build